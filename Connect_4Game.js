//Site Variables
var title = document.getElementById("title");
//Function Variables

//Eventlisteners
title.addEventListener(titleChange, "click");

function titleChange(){
	if(title.color = "Blue") {
		title.color = "Red";	
	} else {
		title.color = "Blue";
	}
};
